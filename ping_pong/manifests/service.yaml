apiVersion: v1
kind: Service
metadata:
  namespace: exercises
  name: pong-svc
spec:
  type: LoadBalancer
  selector:
    app: pong-app
  ports:
    - port: 80
      protocol: TCP
      targetPort: 3000
